# HW14 – эмбеддинги, FAISS, оценка retrieval и mini-RAG по базе знаний

## 1. Кратко: что сделано

- **Какая база знаний использовалась и почему:** Использована учебная база знаний по обработке естественного языка (NLP) и большим языковым моделям (LLM) из 6 документов. Тема выбрана из-за семантической связанности понятий, что позволяет объективно проверить качество retrieval.
- **Как был организован чанкинг документов:** Чанкинг по фиксированному числу символов с `chunk_size=250` и `overlap=40`. Всего создано 12 чанков.
- **Какая embedding-модель или какой способ векторизации использовался:** `paraphrase-multilingual-MiniLM-L12-v2` из библиотеки `sentence-transformers`. Эмбеддинги нормализованы, размерность 384.
- **Что показала базовая проверка retrieval:** На 6 контрольных запросах достигнут `hit@3=1.0` и `recall@3=1.0`. Все релевантные документы найдены в топ-3.
- **Что изменилось после обновления базы знаний:** После добавления 3 документов (RAG, RLHF, fine-tuning) retriever стал корректно находить новые документы. Во всех случаях `changed=True`.
- **Как сработал mini-RAG:** Учебный RAG на основе извлечения последнего предложения из топ-1 чанка показал стабильную работу, возвращая осмысленные ответы с указанием источников.

## 2. Среда и воспроизводимость

- **Python:** 3.11.5
- **faiss / faiss-cpu:** faiss-cpu
- **sentence-transformers / transformers / sklearn:** sentence-transformers, scikit-learn
- **torch (если использовался):** Да (через sentence-transformers)
- **Устройство (CPU/GPU):** CPU
- **Seed:** 42
- **Как запустить:** открыть `HW14.ipynb` и выполнить Run All.

## 3. База знаний и подготовка документов

- **Предметная область:** Natural Language Processing (NLP) и большие языковые модели (LLM).
- **Источник документов:** Составлены вручную.
- **Число исходных документов:** 6
- **Число чанков после разбиения:** 12
- **Какой способ чанкинга использовался:** Разбиение по фиксированному числу символов.
- **Основные параметры чанкинга:** `chunk_size = 250`, `overlap = 40`.
- **Комментарий (3-6 предложений):** База знаний по NLP выбрана из-за высокой семантической связанности понятий. Например, «Трансформеры», «Механизм внимания» и «LLM» тесно переплетены, что создает вызов для retriever — он должен различать эти понятия. Чанкинг с overlap помогает сохранить контекст на границах фрагментов. Добавление новых документов (RAG, RLHF, fine-tuning) позволяет наглядно продемонстрировать необходимость переиндексации.

## 4. Retrieval и индекс

### 4.1. Эмбеддинги и индекс

- **Какая embedding-модель или способ векторизации использовались:** `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`
- **Какая метрика сходства использовалась:** Косинусное сходство (через `IndexFlatIP` с нормализованными векторами).
- **Какой индекс `FAISS` использовался:** `faiss.IndexFlatIP`.
- **Какой `top_k` использовался по умолчанию:** 3.
- **Что показали первые примеры retrieval:** На запрос «Как работает механизм внимания?» retriever вернул чанки из документов `nlp_03` и `nlp_02`, что является корректным результатом. Модель правильно связывает механизм внимания с трансформерами.

### 4.2. Контрольные запросы и оценка retrieval

- **Сколько контрольных запросов использовалось:** 6.
- **Что считалось релевантным ответом:** Совпадение `expected_source` с одним из `doc_id` в выдаче.
- **Какие метрики retrieval считались:** `hit@k`, `recall@k`.
- **Итоговый `hit@k` (для k=3):** 1.0
- **Итоговый `recall@k` (для k=3):** 1.0
- **Дополнительные метрики (если были):** `rank_of_first_relevant`.
- **Какие запросы оказались самыми простыми:** Запросы `q01`, `q02`, `q04`, `q05` — релевантный документ на 1-м месте.
- **Какие запросы оказались самыми проблемными:** Запрос `q03` (ранг 2) и `q06` (ранг 3) — релевантный документ найден, но не на первом месте. Это связано с семантической близостью других документов.

## 5. Эксперимент по параметрам retrieval и обновление базы знаний

- **Какой параметр сравнивался:** `chunk_size`.
- **Какие два варианта сравнивались:** `chunk_size=200` и `chunk_size=350`.
- **Как изменилось качество retrieval:** При `chunk_size=200` `hit@3=0.833`, при `chunk_size=350` `hit@3=1.0`. Более крупные чанки показали лучший результат на данном корпусе.
- **Какой вариант вы выбрали как основной и почему:** Оставлен `chunk_size=250` как компромиссный вариант (12 чанков, отличное качество).
- **Что именно было обновлено в базе знаний:** Добавлены документы: `nlp_07` (RAG), `nlp_08` (RLHF), `nlp_09` (Fine-tuning).
- **Как была выполнена переиндексация:** Повторный чанкинг, вычисление эмбеддингов и перестроение индекса FAISS.
- **Что изменилось в retrieval после обновления базы знаний:** До обновления на запросы о RAG/RLHF/fine-tuning retriever выдавал нерелевантные документы (`nlp_04`, `nlp_06`). После обновления — точные документы (`nlp_07`, `nlp_08`, `nlp_09`) на первых позициях. Все три запроса показали `changed=True`.

## 6. Mini-RAG и результаты

Ссылки на файлы в репозитории:

- Оценка retrieval: `./artifacts/retrieval_eval.csv`
- Примеры ответов mini-RAG: `./artifacts/rag_examples.csv`
- Сравнение до/после обновления: `./artifacts/retrieval_before_after_update.csv`

Короткая сводка:

- **Как формировался контекст для ответа:** Из текста чанков, попавших в `top_k=3`.
- **Какой способ генерации ответа использовался:** Extractive — последнее предложение из самого релевантного чанка.
- **Возвращались ли источники вместе с ответом:** Да, список уникальных `doc_id`.
- **На каких вопросах mini-RAG сработал лучше всего:** «Что такое NLP?» — ответ точно отражает определение из документа.
- **На каких вопросах mini-RAG ошибался или отвечал неуверенно:** «Как работают трансформеры?» — ответ оборван из-за ограничения чанка по символам. «Как улучшить промпт для LLM?» — ответ взят из документа `nlp_04`, а не `nlp_06`, что показывает ограничение extractive-подхода.
- **Какую роль сыграло качество retrieval в итоговом ответе:** Ключевую. Ответ полностью зависит от того, какой чанк оказался на первом месте.

## 7. Анализ

База знаний по NLP оказалась удачной для проверки retrieval. Семантическая близость документов создала реалистичные условия, в которых retriever мог ошибаться (например, `q06` нашёлся только на 3-м месте). Чанкинг по символам с `overlap=40` показал себя хорошо для коротких текстов, но на более длинных документах мог бы разрывать смысловые блоки.

Формальная оценка с помощью `hit@k` и `recall@k` позволила объективно сравнить конфигурации. Эксперимент с `chunk_size` показал, что более крупные чанки (350) дали лучший результат на данном корпусе, что может быть связано с сохранением целостности определений.

Обновление базы знаний наглядно продемонстрировало необходимость переиндексации. Без неё retriever «не знал» о новых документах. После переиндексации качество ответов на новые запросы возросло кардинально.

Наиболее показательные ошибки mini-RAG: обрыв ответа из-за фиксированного размера чанка и выбор не того источника для общего вопроса. Это демонстрирует ограничения простого extractive-подхода и показывает, что полноценная LLM с доступом ко всему контексту была бы эффективнее.

## 8. Итоговый вывод

В качестве базового retrieval-конвейера я бы оставил конфигурацию: `chunk_size=250`, `overlap=40`, модель `paraphrase-multilingual-MiniLM-L12-v2`. Она показала `hit@3=1.0` на тестовых запросах. Главный вывод: качество retrieval необходимо измерять объективными метриками (`hit@k`, `recall@k`), а не субъективным впечатлением. В RAG-системах генератор полностью зависит от retrieval: если контекст не найден, ответ будет неверным.

## 9. Приложение (опционально)

Дополнительные эксперименты не проводились.